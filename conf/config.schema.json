{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "logInternalErrors": {
      "description": "Whether errors in the MongoDBLoggerModule module should be logged using the default logger. Can cause mass logs in some cases.",
      "type": "boolean",
      "default": false
    },
    "maxLogCount": {
      "description": "The maximum number of logs stored at any one time (the oldest logs will be replaced first).",
      "type": "number",
      "default": 5000
    }
  }
}
