# adapt-authoring-mongodblogger

Persists application log messages to MongoDB, storing them via the [mongodb](../adapt-authoring-mongodb) module instance so logs can be queried and reviewed after the fact.

Extends `AbstractApiModule` from [adapt-authoring-core](../adapt-authoring-core).
